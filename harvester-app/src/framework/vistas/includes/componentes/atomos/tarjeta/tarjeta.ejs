<link href='<%= rutaBase %>src/framework/vistas/includes/componentes/atomos/tarjeta/tarjeta.css' rel='stylesheet' />
<div class="tarjeta tarjeta-<%= color%>" id="<%= id %>">
    <% elementos.forEach((elemento)=> { %>
        <%-include(elemento.ruta, elemento.parametros)%>
    <% }) %>
</div>

<% /*
Nombres de colores válidos:
    - gris
    - blanco
    - azul

Parámetros:
    - id: 'tarjeta'
    - color: color de la tarjeta (gris o blanco)
    - elementos: [{
        ruta: ''
        parametros: {}
    }]

*/ %>